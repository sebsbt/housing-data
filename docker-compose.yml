services:
  web:
    build: .
    ports:
      # Bind host side to loopback so the container is not reachable from your LAN by default
      - "127.0.0.1:8080:8080"
    environment:
      NODE_ENV: production
      PORT: "8080"
